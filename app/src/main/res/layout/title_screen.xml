<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- GLSurfaceView располагается внизу RelativeLayout -->
    <android.opengl.GLSurfaceView
        android:id="@+id/glSurfz"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_alignParentBottom="true" />

    <!-- ImageView -->
    <ImageView
        android:id="@+id/imageView_message_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:contentDescription="@string/cats_back"
        android:scaleType="fitCenter"
        android:src="@drawable/cats"
        android:layout_above="@id/glSurfz" />

    <!-- TextView -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="40dp"
        android:text="@string/welcome"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="34sp"
        android:textStyle="bold" />

    <!-- Кнопки располагаются поверх GLSurfaceView -->
    <Button
        android:id="@+id/welcomeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_above="@id/glSurfz"
        android:layout_marginEnd="46dp"
        android:layout_marginBottom="10dp"
        android:text="@string/go_to_come"
        android:textColor="@color/white"
        android:textSize="16sp" />

    <Button
        android:id="@+id/loginButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_above="@id/glSurfz"
        android:layout_marginStart="57dp"
        android:layout_marginBottom="10dp"
        android:text="login"
        android:textColor="@color/white"
        android:textSize="16sp" />

</RelativeLayout>
